# =========================================================
# Prometheus scrape config for MarketSentinel
# Place at: app/monitoring/prometheus.yml
#
# FIX: target changed from 'inference:8000' to 'api:8000'
#      docker-compose service is named 'api' not 'inference'
#      Prometheus was scraping nothing before this fix.
#
# Usage with Docker Compose:
#   prometheus:
#     image: prom/prometheus
#     volumes:
#       - ./app/monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
#     ports:
#       - "9090:9090"
# =========================================================

global:
  scrape_interval: 30s
  evaluation_interval: 30s
  external_labels:
    project: marketsentinel
    env: production

scrape_configs:
  - job_name: marketsentinel_api
    static_configs:
      - targets: ['api:8000']
    metrics_path: /metrics
    scheme: http
    scrape_interval: 15s
    scrape_timeout: 10s