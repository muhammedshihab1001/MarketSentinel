# =========================================================
# MarketSentinel — LLM Requirements
# Only installed when LLM_ENABLED=true
#
# Build with LLM support:
#   docker build --build-arg INSTALL_LLM=true -f docker/inference.Dockerfile .
#
# Not included in base.txt to keep inference image lean (~35MB saved).
# =========================================================

openai==2.21.0