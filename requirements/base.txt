# =========================================================
# MarketSentinel — Base Requirements
# Shared across inference, training, and CI
# =========================================================

# ── Web framework ─────────────────────────────────────────
fastapi==0.112.2
uvicorn==0.30.6
python-multipart==0.0.9
orjson==3.10.6
starlette==0.38.6

# ── Data validation ───────────────────────────────────────
pydantic==2.8.2
pydantic_core==2.20.1
annotated-types==0.7.0

# ── Database ──────────────────────────────────────────────
SQLAlchemy==2.0.41
psycopg2-binary==2.9.10
alembic==1.15.1

# ── Cache ─────────────────────────────────────────────────
redis==5.0.7

# ── ML core ───────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
xgboost==2.1.1
joblib==1.4.2

# ── Market data ───────────────────────────────────────────
yfinance==1.2.0
curl_cffi==0.13.0
requests==2.32.5
httpx==0.27.0

# ── Observability ─────────────────────────────────────────
prometheus_client==0.20.0
psutil==6.0.0

# ── LLM (optional — disabled by default via LLM_ENABLED=false) ──
openai==2.21.0

# ── Config / env ──────────────────────────────────────────
python-dotenv==1.0.1
PyYAML==6.0.3
click==8.3.1

# ── Async runtime ─────────────────────────────────────────
anyio==4.4.0
sniffio==1.3.1
httpcore==1.0.9
httptools==0.6.1

# ── Serialisation / time ──────────────────────────────────
python-dateutil==2.9.0.post0
pytz==2025.2
tzdata==2025.3
packaging==24.1
typing_extensions==4.12.2

# ── Network / security ────────────────────────────────────
cryptography==45.0.7
certifi==2026.1.4
idna==3.11
urllib3==2.6.3
charset-normalizer==3.4.4

# ── Utilities ─────────────────────────────────────────────
tqdm==4.66.5
tenacity==9.1.4
filelock==3.20.3
frozendict==2.4.7
multitasking==0.0.12
six==1.17.0

# ── Auth (JWT + bcrypt password hashing) ──────────────────
# IMPORTANT: bcrypt must be pinned to 4.0.1
# passlib 1.7.4 is not compatible with bcrypt 4.1+
bcrypt==4.0.1
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0